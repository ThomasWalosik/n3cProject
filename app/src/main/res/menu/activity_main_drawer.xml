<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      android:id="@+id/nav_menu"
      tools:showIn="navigation_view">
<!--
    <group android:checkableBehavior="single">
         * app:actionLayout="@layout/layout_image_remplacer_remove" *
        <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_menu_gallery"
                android:title="@string/menu_home"/>
         * app:actionLayout="@layout/ordonnance_item" *
        <item
                android:id="@+id/nav_ordonnance"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_ordonnance"/>
         * app:actionLayout="@layout/meditation_item" *
        <item
                android:id="@+id/nav_meditation"
                android:icon="@drawable/ic_menu_manage"
                android:title="@string/menu_meditation"/>
    </group>
        -->
</menu>